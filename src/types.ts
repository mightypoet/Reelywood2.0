export type Course = {
  id: string;
  title: string;
  description: string;
  image: string;
  icon?: string;
};

export type Testimonial = {
  id: string;
  author: string;
  content: string;
  rating: number;
};
